services:
  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ship
      POSTGRES_PASSWORD: ship
      POSTGRES_DB: ship_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
