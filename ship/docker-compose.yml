services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ship
      POSTGRES_PASSWORD: ship
      POSTGRES_DB: ship_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
